# Rename this file to .env and fill in values
TELEGRAM_BOT_TOKEN=123456:ABCDEF_your_token_here
# Comma-separated list of chat IDs to notify (optional). If empty, the bot will register chats via /start.
NOTIFY_CHAT_IDS=
# Fetch interval in minutes
FETCH_INTERVAL_MIN=10
# Data source provider: bitinfocharts (default). You can add others in providers.py
PROVIDER=bitinfocharts
# Optional: HTTP proxy for requests, e.g., http://user:pass@host:port
HTTP_PROXY=
